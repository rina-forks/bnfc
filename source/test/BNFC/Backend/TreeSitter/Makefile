.PHONY: all test

all:
	bash -ec 'for f in *.cf; do js="$${f%.cf}".expected.js && rm -rfv $$js && $(MAKE) $$js; done'

%.expected.js: %.cf
	cabal run bnfc -- --tree-sitter $<
	grep -v 'File generated by' grammar.js > $@
	rm -rf grammar.js grammar.js.bak

test-generate: *.expected.js
	bash -xec 'd=`pwd`; cd `mktemp -d`; for f in "$$@"; do tree-sitter generate $$d/$$f; done' x $^


.PHONY: all
all:
	bash -c 'for f in *.cf; do touch "$${f%.cf}".expected.js; touch $$f; done'
	$(MAKE) *.expected.js

%.expected.js: %.cf
	cabal run bnfc -- --tree-sitter $<
	grep -v 'File generated by' grammar.js > $@
	rm -rf grammar.js

